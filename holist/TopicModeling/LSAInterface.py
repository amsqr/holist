import gensim

import logging
logging.basicConfig(format='%(asctime)s : %(levelname)s : %(message)s', level=logging.INFO)

id2word = None #this needs to be a dictionary or other object mapping the IDs to words (in the datatbase, maybe)

